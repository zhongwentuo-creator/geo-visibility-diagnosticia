# V1.5 节点 5：跨设备与 24 小时稳定性验收

> 本文件记录节点 5 的执行事实、时间窗口和交接入口；任务状态以 `plan.md` 为准，最终验收结论以 `ACCEPTANCE.md` 为准，运行数据以 `PROJECT_NODE_RECORDS.md` 为准。

## 1. 节点目标

确认已通过节点 4 的公网版本，在真实用户设备和 24 小时稳定性窗口内仍可完成诊断与读取报告：

1. 外网桌面端可完成一次用户工作台诊断并打开 JSON、HTML 报告。
2. 手机 4G 环境可完成一次诊断，完成态、JSON 与 HTML 报告均可使用。
3. 从手机任务开始满 24 小时后，在未改动生产版本的前提下再次完成真实诊断；没有阻断错误。

## 2. 已完成证据

| 验收面 | 时间与环境 | 任务 / 结果 | 报告与结论 |
|---|---|---|---|
| 桌面端 | 2026-07-17；公网工作台 | `GEO-20260716-235520-a88301`，`success`，9 阶段完成；Stage 4 完成 18 条查询，AIVO 83 | JSON 200（31,089 bytes）；HTML 200（52,683 bytes）；工作台、JSON 页与下载 HTML 同时留有操作证据。 |
| 手机端 | 2026-07-17；iPhone / Safari / 4G | `GEO-20260717-002702-daf325`，`success`，9 阶段完成；Stage 4 完成 18/18 查询，AIVO 78 | 完成页、JSON 打开页与 HTML 报告截图已保存；HTML 在手机端可阅读。JSON 原始内容为有效 UTF-8，单张截图的乱码不作为接口故障结论。 |
| T+24 小时复测 | 2026-07-18；距手机任务启动约 25 小时 20 分钟 | `GEO-20260718-014657-f8732c`，`success`，9 阶段完成；Stage 4 完成 18/18 查询，耗时 106.914 秒，AIVO 82 | JSON 200（31,421 bytes）；HTML 200（52,306 bytes）；附带 HTML 文件为 UTF-8，标题和品牌信息正确。 |

## 3. 判定与运行边界

- 节点 5 **通过**：桌面、手机与 T+24 小时复测均没有阻断错误。
- Stage 4 仍可能因深度搜索模型耗时约 60–110 秒；本轮通过 heartbeat、查询进度和状态恢复保持可见，并未造成诊断或报告读取失败。
- Stage 3、Stage 6 的 `degraded` / `completed` 结果按既有引擎降级契约处理，不等同于任务失败；每次任务的最终状态均为 `success`。
- 本次结项不改变 V1.0 引擎、模型、查询数量或评分逻辑。进程内任务状态、单进程串行锁和外部服务依赖仍是后续版本的已知限制，见 `ISSUE_BACKLOG.md` 与 `SDD.md`。

## 4. 后续读取顺序

1. `AGENTS.md` 第 0 节。
2. `MEMORY.md` 的“当前有效事实”。
3. 本文件确认设备与稳定性证据。
4. `ACCEPTANCE.md`、`plan.md`、`PROJECT_NODE_RECORDS.md` 确认最终发布状态。
5. 若讨论后续优化，再读取 `ISSUE_BACKLOG.md`、`SDD.md`，不要把技术债误判为本次发布阻断。

推荐触发语：

> 请读取 `AGENTS.md`、`MEMORY.md` 和 `v1.5/docs/NODE5_EXECUTION.md`，复核《GEO 可见度诊断师_1.5》的跨设备与 24 小时稳定性证据；先报告当前发布状态，再决定是否进入后续优化。
